{
  "name": "devops-capstone-expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A server-rendered expense tracker built for the DevOps Essentials capstone project.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ejs": "6.0.1",
    "express": "5.2.1",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "supertest": "7.2.2"
  }
}
